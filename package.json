{
  "name": "my-bike",
  "version": "1.0.0",
  "description": "Find your bike size in seconds",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "build": "cap build ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
